# Distribution builds and archives
dist-*/
server/dist/
*.zip
*.tar.gz
*.exe
releases/
icad_tone_detection-main/
rdio-scanner-master/

# Debug files and data
server/debug-audio/
server/tone-keyword-debug.log

# Large model files (download separately)
server/models/*.bin

# Config files (use templates instead)
/config-sync/
server/.geocodify-api-key
rdio-scanner.json
rdio-scanner(2).json
server/ThinlineRadio/

# System files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.cursor/

# Node
node_modules/
create-ico-temp.js
*.swp
*.swo
*~

# Local build and run scripts (each environment has its own)
build-all.sh
build-linux.sh
build-macos.sh
build-windows.sh
run-server.sh

# Temporary files
*.log
*.tmp
.gomodcache/
.gomodcache-build/

# Docker
.env

# Local ops / migrate / one-off scripts (never commit secrets from these trees)
/scripts/
.nextcloud-migrate-staging/
.nextcloud-vps-migrate-staging/
.mail-migrate-staging/

# Local SSH keys + host connection notes (never commit)
ops-private/

# Local server ops helpers and ad-hoc tools (keep icad_tone_bridge.py tracked)
server/scripts/*.sh
server/scripts/launchd/
server/cmd/streetscan_prototype/
server/*_main.go
server/setup_*_main.go
server/mapping/tmp_*.go

# Built webapp / local binaries (produced by build-all; not source)
server/webapp/
/thinline-radio-*
/thinline-radio

TLR-Tones2Active911-main/
docker/data/
docker/logs/
docker/config/*.pem
docker/config/*.key
docker/config/*.json
docker/config/*.ini
!docker/config/README.md
!docker/init-db/README.md
!.github/

# Separate repositories (Git managed independently)
relay-server/
nominatim-gateway/
transcript-collector/
ThinlineRadio-Mobile/
ThinlineRadio-Website/
ThinlineRadio-Web/

# Website build artifacts (if working inside ThinlineRadio-Website locally)
ThinlineRadio-Website/dist/
ThinlineRadio-Website/*.zip
ThinlineResponder/
whisper/
# Separate CM / AlertPage repo only (not client admin/central-management/)
/central-management/
TonesToActive/
tlr-time-sync/

# Config files

ThinLineRadioV7-config.json
node_modules

# TLR DB migrate secrets
scripts/tlr-db-migrate/migrate.env
